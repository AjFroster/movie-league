# Loaded only by `vite --mode test`, and it outranks .env.local, which is where a real
# Clerk key lives. Empty means the browser tests run in local mode: one real user, with
# permissions still enforced server-side. Committed on purpose -- there is no secret here,
# and the tests are wrong without it.
VITE_CLERK_PUBLISHABLE_KEY=
