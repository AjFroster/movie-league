---
phase: 01-ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/index.html
  - frontend/src/styles.css
autonomous: true
requirements: [UI-03]

must_haves:
  truths:
    - "App-wide background, text, and accent colors match the locked design tokens (#0a0a0f bg, #e0a339 accent, #f2545b destructive, #5b9bd5 informational status)"
    - "Bebas Neue font, the gradient-text header treatment, and all rank-1/2/3 box-shadow glow rules are fully removed from the stylesheet — zero box-shadow declarations remain anywhere"
    - "Playfair Display and JetBrains Mono fonts load via the Google Fonts link in index.html, replacing Bebas Neue"
    - "Locked line-heights are implemented as tokens and applied to their roles (Label 1.4, Heading 1.2, Display 1.15) instead of every role inheriting body's 1.5"
    - "Desktop two-column layout survives: the @media (min-width: 900px) app-layout rule is present in the final file"
  artifacts:
    - path: "frontend/src/styles.css"
      provides: "Design tokens plus every CSS class consumed by the wave-2 plans (app-layout, this-week, roster-table, status-pill, movie-detail, penalty-note, player-meta-line, etc.)"
      contains: "--accent: #e0a339;"
    - path: "frontend/index.html"
      provides: "Font loading for the new two-family type system"
      contains: "Playfair+Display"
  key_links:
    - from: "frontend/src/styles.css"
      to: "frontend/src/components/*.jsx (Plans 01-02, 01-03, 01-04)"
      via: "Shared CSS class-name contract defined here and consumed verbatim by later plans"
      pattern: "roster-row|status-pill|movie-hero|this-week|penalty-note|player-meta-line"
---

<objective>
Rebuild `frontend/src/styles.css` and the Google Fonts link in `frontend/index.html` from scratch to match the "Movie League" dark design system locked in `01-UI-SPEC.md`: new color tokens, new two-family type system with locked line-heights, new spacing scale, and every CSS class the roster-table, status-pill, and movie-detail views will need. This plan produces zero visible UI change by itself (no JSX is touched) — it produces the token/class contract that Plans 01-02, 01-03, and 01-04 build against in parallel.

Purpose: Establish a single source of truth for design tokens and component CSS so the three downstream JSX plans can be executed independently (different files, same wave) without needing to invent or negotiate class names.
Output: `frontend/src/styles.css` fully rewritten; `frontend/index.html` font link updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-ui-redesign/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace fonts and rewrite design tokens + global layout CSS (Part A)</name>
  <files>frontend/index.html, frontend/src/styles.css</files>
  <read_first>
    frontend/index.html
    frontend/src/styles.css
    .planning/phases/01-ui-redesign/01-UI-SPEC.md (Design System, Spacing Scale, Typography, Color, Layout & Responsive Notes, Superseded Tokens sections)
  </read_first>
  <action>
In `frontend/index.html`, replace the existing Google Fonts `<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue...">` href value with exactly:
`https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=JetBrains+Mono:wght@400;700&family=Inter:wght@400;700&display=swap`
Keep the two `<link rel="preconnect">` lines unchanged.

In `frontend/src/styles.css`, REPLACE THE ENTIRE FILE CONTENTS with the following (this is Part A — Task 2 appends Part B to the end of this same file, do not add a closing comment or trailing media query here, Task 2 owns the responsive block):

```css
:root {
  --bg: #0a0a0f;
  --surface: #121218;
  --surface-raised: #17171f;
  --accent: #e0a339;
  --destructive: #f2545b;
  --status-info: #5b9bd5;
  --text: #e4e7f5;
  --text-dim: #8890ab;
  --text-faint: #4d5372;
  --border: #1c1c26;
  --border-bright: #2a2a38;

  --font-display: 'Playfair Display', Georgia, serif;
  --font-label: 'JetBrains Mono', 'IBM Plex Mono', monospace;
  --font-body: 'Inter', system-ui, sans-serif;

  --space-xs: 4px;
  --space-sm: 8px;
  --space-md: 16px;
  --space-lg: 24px;
  --space-xl: 32px;
  --space-2xl: 48px;

  --fs-body: 14px;
  --fs-label: 11px;
  --fs-heading: 20px;
  --fs-display: 36px;

  --lh-body: 1.5;
  --lh-label: 1.4;
  --lh-heading: 1.2;
  --lh-display: 1.15;

  --row-min-height: 44px;
  --dot-size: 8px;
}

* { box-sizing: border-box; }

body {
  margin: 0;
  background: var(--bg);
  color: var(--text);
  font-family: var(--font-body);
  font-size: var(--fs-body);
  line-height: var(--lh-body);
  -webkit-font-smoothing: antialiased;
}

@media (prefers-reduced-motion: reduce) {
  * { animation: none !important; transition: none !important; }
}

/* -- Layout -- */
.app {
  max-width: 1100px;
  margin: 0 auto;
  padding: 0 var(--space-lg) 80px;
}

.header {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
  padding: var(--space-xl) 0 var(--space-lg);
  border-bottom: 1px solid var(--border);
  margin-bottom: var(--space-2xl);
}
.header-mark {
  width: var(--dot-size);
  height: var(--dot-size);
  background: var(--accent);
  border-radius: 2px;
  flex-shrink: 0;
}
.header h1 {
  font-family: var(--font-label);
  font-size: var(--fs-heading);
  font-weight: 700;
  line-height: var(--lh-heading);
  letter-spacing: 0.08em;
  text-transform: uppercase;
  margin: 0;
  color: var(--text);
}
.header p {
  margin: var(--space-xs) 0 0;
  color: var(--text-dim);
  font-family: var(--font-label);
  font-size: var(--fs-label);
  line-height: var(--lh-label);
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.state-msg {
  text-align: center;
  color: var(--text-dim);
  padding: 60px 0;
  font-family: var(--font-label);
  font-size: var(--fs-label);
  line-height: var(--lh-label);
  letter-spacing: 0.05em;
}

.empty-state { text-align: center; padding: 60px var(--space-lg); }
.empty-state-heading {
  font-family: var(--font-label);
  font-size: var(--fs-heading);
  font-weight: 700;
  line-height: var(--lh-heading);
  color: var(--text);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}
.empty-state-body { margin-top: var(--space-sm); color: var(--text-dim); font-size: var(--fs-body); }

.app-layout {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--space-xl);
}
@media (min-width: 900px) {
  .app-layout { grid-template-columns: 1fr 320px; align-items: start; }
}
.app-main { min-width: 0; }

/* -- THIS WEEK sidebar (static, illustrative) -- */
.this-week {
  background: var(--surface);
  border: 1px solid var(--border);
  padding: var(--space-lg);
}
.this-week-header {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: var(--space-sm);
  margin-bottom: var(--space-md);
}
.this-week-title {
  font-family: var(--font-label);
  font-weight: 700;
  font-size: var(--fs-heading);
  line-height: var(--lh-heading);
  color: var(--text-dim);
}
.illustrative-note {
  font-family: var(--font-label);
  font-size: var(--fs-label);
  line-height: var(--lh-label);
  color: var(--text-faint);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}
.this-week-item { padding: var(--space-md) 0; border-top: 1px solid var(--border); }
.this-week-item:first-of-type { border-top: none; padding-top: 0; }
.this-week-tag {
  font-family: var(--font-label);
  font-size: var(--fs-label);
  line-height: var(--lh-label);
  color: var(--text-dim);
  text-transform: uppercase;
  letter-spacing: 0.08em;
}
.this-week-item-headline { margin-top: var(--space-xs); font-weight: 700; color: var(--text); font-size: var(--fs-body); }
.this-week-item-body { margin-top: var(--space-xs); color: var(--text-dim); font-size: var(--fs-body); }

/* -- Leaderboard -- */
.leaderboard { display: flex; flex-direction: column; gap: var(--space-lg); }
```

Do not carry over any token, class, or rule from the old file that is not listed above (Bebas Neue, `.header h1` gradient/background-clip treatment, old `--rank-1/2/3/other` tokens, old `--clr-*` score-chip tokens, `.movie-grid`, `.player-score-bar*`, `.score-chip*`, `.rounds-badge`, old `.movie-card*` rules, etc. are all superseded — Task 2 defines their modern replacements where needed, everything else is deleted).

The `@media (min-width: 900px)` block above is REQUIRED — it is the desktop two-column layout UI-SPEC's "Layout & Responsive Notes" mandates. Never delete it to make a grep count smaller.
  </action>
  <verify>
    <automated>! grep -q "Bebas" frontend/index.html && grep -q "Playfair+Display" frontend/index.html && grep -q -- "--lh-display: 1.15;" frontend/src/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Bebas" frontend/index.html` == 0
    - `grep -c "Playfair+Display" frontend/index.html` == 1
    - `grep -c "JetBrains+Mono" frontend/index.html` == 1
    - `grep -c -- "--accent: #e0a339;" frontend/src/styles.css` == 1
    - `grep -c -- "--bg: #0a0a0f;" frontend/src/styles.css` == 1
    - `grep -c -- "--lh-label: 1.4;" frontend/src/styles.css` == 1
    - `grep -c -- "--lh-heading: 1.2;" frontend/src/styles.css` == 1
    - `grep -c -- "--lh-display: 1.15;" frontend/src/styles.css` == 1
    - `grep -c "Bebas Neue" frontend/src/styles.css` == 0
    - `grep -c "box-shadow" frontend/src/styles.css` == 0
    - `grep -c "app-layout" frontend/src/styles.css` -ge 1
    - `grep -c "@media (min-width: 900px)" frontend/src/styles.css` == 1
    - `grep -c "this-week" frontend/src/styles.css` -ge 1
  </acceptance_criteria>
  <done>index.html loads Playfair Display + JetBrains Mono + Inter only; styles.css contains the full token set (including the three locked line-height tokens), base rules, app layout grid with its 900px desktop media query, header, empty/state messages, and THIS WEEK widget CSS with no leftover Bebas Neue/box-shadow/old rank-color rules.</done>
</task>

<task type="auto">
  <name>Task 2: Append roster table, status pill, and movie-detail CSS (Part B)</name>
  <files>frontend/src/styles.css</files>
  <read_first>
    frontend/src/styles.css (as left by Task 1 — verify Part A is present before appending)
    .planning/phases/01-ui-redesign/01-UI-SPEC.md (Component & Data Mapping, Status Pill Token Table, Color, Typography sections)
  </read_first>
  <action>
APPEND the following to the end of `frontend/src/styles.css` (after the `.leaderboard` rule Task 1 wrote — do not duplicate or remove anything Task 1 wrote):

```css
/* -- Player Card -- */
.player-card {
  background: var(--surface);
  border: 1px solid var(--border);
  overflow: hidden;
}
.player-card.rank-1 { border-top: 2px solid var(--accent); }

.player-header {
  display: flex;
  align-items: center;
  gap: var(--space-lg);
  padding: var(--space-lg);
  border-bottom: 1px solid var(--border);
  flex-wrap: wrap;
}
.player-rank {
  font-family: var(--font-label);
  font-weight: 700;
  font-size: var(--fs-heading);
  line-height: var(--lh-heading);
  width: 40px;
  flex-shrink: 0;
  color: var(--text-dim);
}
.player-card.rank-1 .player-rank { color: var(--accent); }
.player-identity { flex: 1; min-width: 200px; }
.player-meta-line {
  font-family: var(--font-label);
  font-size: var(--fs-label);
  line-height: var(--lh-label);
  color: var(--text-dim);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  margin-bottom: var(--space-xs);
}
.player-name {
  font-family: var(--font-display);
  font-size: var(--fs-display);
  font-weight: 700;
  line-height: var(--lh-display);
  color: var(--text);
}
.player-stats {
  display: flex;
  gap: var(--space-lg);
  flex-wrap: wrap;
}

/* -- Stat block (reused: player header + movie detail stat strip) -- */
.stat-block {
  display: flex;
  flex-direction: column;
  gap: 2px;
  min-width: 64px;
}
.stat-block-label {
  font-family: var(--font-label);
  font-size: var(--fs-label);
  line-height: var(--lh-label);
  color: var(--text-dim);
  text-transform: uppercase;
  letter-spacing: 0.08em;
}
.stat-block-value {
  font-family: var(--font-label);
  font-weight: 700;
  font-size: var(--fs-heading);
  line-height: var(--lh-heading);
  color: var(--text);
}
.stat-block-value.positive { color: var(--accent); }
.stat-block-value.negative { color: var(--destructive); }
.stat-block-caption {
  font-family: var(--font-label);
  font-size: var(--fs-label);
  line-height: var(--lh-label);
  color: var(--text-dim);
  letter-spacing: 0.02em;
}

/* -- Roster table -- */
.roster-table { display: flex; flex-direction: column; }
.roster-header-row, .roster-row {
  display: grid;
  grid-template-columns: 64px 1fr 160px 64px 72px;
  column-gap: var(--space-md);
  align-items: center;
  min-height: var(--row-min-height);
  padding: 0 var(--space-lg);
  border-bottom: 1px solid var(--border);
}
.roster-header-row {
  font-family: var(--font-label);
  font-size: var(--fs-label);
  line-height: var(--lh-label);
  color: var(--text-dim);
  text-transform: uppercase;
  letter-spacing: 0.08em;
}
.roster-header-row > span:nth-child(4),
.roster-header-row > span:nth-child(5) { text-align: right; }

.roster-row { cursor: pointer; }
.roster-row:not(.pending):hover { background: var(--surface-raised); }
.roster-row:focus-visible { outline: 2px solid var(--accent); outline-offset: -2px; }
.roster-row.expanded { background: var(--surface-raised); border-bottom-color: var(--border-bright); }

.roster-col-round { font-family: var(--font-label); font-size: var(--fs-label); line-height: var(--lh-label); color: var(--text-dim); }
.roster-col-title { font-family: var(--font-body); font-weight: 700; font-size: var(--fs-body); color: var(--text); }
.roster-col-title.dim { color: var(--text-faint); font-weight: 400; font-style: italic; }
.roster-col-pts { font-family: var(--font-label); font-weight: 700; font-size: var(--fs-heading); line-height: var(--lh-heading); text-align: right; }
.roster-col-pts.positive { color: var(--accent); }
.roster-col-pts.negative { color: var(--destructive); }
.roster-col-pts.zero { color: var(--text-dim); }
.roster-col-watched { font-family: var(--font-body); font-size: var(--fs-body); color: var(--text-dim); text-align: right; }

/* pending rows are dimmed; each column sets its own color, so target the columns
   directly — a color declaration on .roster-row.pending alone would never win */
.roster-row.pending { cursor: default; }
.roster-row.pending .roster-col-round,
.roster-row.pending .roster-col-title,
.roster-row.pending .roster-col-pts,
.roster-row.pending .roster-col-watched,
.roster-row.pending .status-label { color: var(--text-faint); }

/* -- Status pill -- */
.status-pill { display: inline-flex; align-items: center; gap: var(--space-xs); }
.status-dot { width: var(--dot-size); height: var(--dot-size); border-radius: 50%; flex-shrink: 0; }
.status-dot.no-pick { background: var(--text-dim); }
.status-dot.awaiting { background: var(--status-info); }
.status-dot.at-risk { background: var(--destructive); }
.status-dot.scored { background: var(--accent); }
.status-label { font-family: var(--font-label); font-size: var(--fs-label); line-height: var(--lh-label); color: var(--text-dim); text-transform: uppercase; letter-spacing: 0.08em; }

/* -- Movie detail (expanded) -- */
.movie-detail { padding: var(--space-lg); display: flex; flex-direction: column; gap: var(--space-lg); }
.movie-hero { height: 120px; background: linear-gradient(135deg, var(--surface), var(--surface-raised)); }
.movie-detail-title { font-family: var(--font-display); font-size: var(--fs-display); font-weight: 700; line-height: var(--lh-display); color: var(--text); margin: var(--space-md) 0 0; }
.movie-detail-meta { font-family: var(--font-label); font-size: var(--fs-label); line-height: var(--lh-label); color: var(--text-dim); text-transform: uppercase; letter-spacing: 0.06em; margin-top: var(--space-xs); }

.stat-strip { display: flex; flex-wrap: wrap; }
.stat-strip .stat-block { padding: 0 var(--space-lg); border-right: 1px solid var(--border); }
.stat-strip .stat-block:first-child { padding-left: 0; }
.stat-strip .stat-block:last-child { border-right: none; }

.detail-columns { display: grid; grid-template-columns: 2fr 1fr 1fr; gap: var(--space-lg); }
.detail-col { display: flex; flex-direction: column; min-width: 0; }
.detail-section-title {
  font-family: var(--font-label);
  font-size: var(--fs-heading);
  font-weight: 700;
  line-height: var(--lh-heading);
  color: var(--text-dim);
  margin-bottom: var(--space-sm);
}

.points-ledger { display: flex; flex-direction: column; gap: var(--space-sm); }
.ledger-row { display: grid; grid-template-columns: 100px 1fr 64px; align-items: center; gap: var(--space-sm); }
.ledger-label { font-family: var(--font-label); font-size: var(--fs-label); line-height: var(--lh-label); color: var(--text-dim); text-transform: uppercase; letter-spacing: 0.06em; }
.ledger-bar-track { display: block; height: 6px; background: var(--border); overflow: hidden; }
.ledger-bar-fill { display: block; height: 100%; }
.ledger-bar-fill.positive { background: var(--accent); }
.ledger-bar-fill.negative { background: var(--destructive); }
.ledger-value { font-family: var(--font-label); font-weight: 700; font-size: var(--fs-body); text-align: right; }
.ledger-value.positive { color: var(--accent); }
.ledger-value.negative { color: var(--destructive); }

/* penalty explanation text under the ledger — Body role per UI-SPEC Typography */
.penalty-note {
  font-family: var(--font-body);
  font-size: var(--fs-body);
  color: var(--text-dim);
  margin-top: var(--space-sm);
}

.campaign-tracker { display: flex; flex-direction: column; gap: var(--space-md); }
.tracker-item { display: flex; gap: var(--space-sm); }
.tracker-dot { width: var(--dot-size); height: var(--dot-size); border-radius: 50%; margin-top: 4px; flex-shrink: 0; }
.tracker-dot.amber { background: var(--accent); }
.tracker-dot.blue { background: var(--status-info); }
.tracker-dot.gray { background: var(--text-faint); }
.tracker-headline { font-weight: 700; color: var(--text); font-size: var(--fs-body); }
.tracker-detail { color: var(--text-dim); font-size: var(--fs-label); line-height: var(--lh-label); font-family: var(--font-label); }

.ownership-callout { border: 1px solid var(--accent); padding: var(--space-md); background: var(--surface-raised); font-size: var(--fs-body); color: var(--text); }
.ownership-callout strong { color: var(--accent); }

.card-error { padding: var(--space-md) var(--space-lg); color: var(--destructive); font-family: var(--font-label); font-size: var(--fs-label); }

/* -- Responsive -- */
@media (max-width: 600px) {
  .player-name, .movie-detail-title { font-size: 28px; }
  .roster-header-row, .roster-row { grid-template-columns: 48px 1fr 120px 56px 56px; padding: 0 var(--space-md); }
  .stat-strip { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-md); }
  .stat-strip .stat-block { border-right: none; padding: 0; }
  .detail-columns { grid-template-columns: 1fr; }
}
```

This is the ONLY `@media (max-width: 600px)` block in the file — Task 1 did not add one.

Two rules above are load-bearing and must be written exactly as shown:
- `.ledger-bar-fill { display: block; height: 100%; }` — without `display: block`, the fill is a non-replaced inline box and BOTH its `height: 100%` and the inline `width: N%` that Plan 01-04 sets are silently ignored, so every points-ledger bar renders as nothing while the build still passes.
- The `.roster-row.pending .roster-col-*` selector list — a bare `.roster-row.pending { color: ... }` never applies, because every column class sets its own `color`.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "roster-header-row" frontend/src/styles.css` -ge 1
    - `grep -c "status-dot.at-risk" frontend/src/styles.css` == 1
    - `grep -c "movie-hero" frontend/src/styles.css` == 1
    - `grep -c "campaign-tracker" frontend/src/styles.css` == 1
    - `grep -c "ownership-callout" frontend/src/styles.css` -ge 1
    - `grep -c "\.ledger-bar-fill { display: block; height: 100%; }" frontend/src/styles.css` == 1
    - `grep -c "\.roster-row\.pending \.roster-col-title" frontend/src/styles.css` == 1
    - `grep -c "player-meta-line" frontend/src/styles.css` == 1
    - `grep -c "penalty-note" frontend/src/styles.css` == 1
    - `grep -c "box-shadow" frontend/src/styles.css` == 0
    - `grep -c "rank-2\|rank-3" frontend/src/styles.css` == 0
    - `grep -c "@media (max-width: 600px)" frontend/src/styles.css` == 1
    - `grep -c "@media (min-width: 900px)" frontend/src/styles.css` == 1
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>styles.css contains the complete class contract (player-card + meta line, roster-table with working pending dimming, status-pill, movie-detail, points-ledger with block-level bar fills, penalty-note, campaign-tracker, ownership-callout) with no box-shadow anywhere and both responsive breakpoints intact; `npm run build` succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser (React SPA) -> FastAPI backend (`/api/*`) | Existing untrusted-input boundary; unchanged by this plan — no endpoints, request shapes, or write paths are touched (CSS/font-link only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-01 | Information Disclosure | `frontend/src/styles.css`, `frontend/index.html` | accept | Pure presentational change (colors/fonts/spacing/class scaffolding). No data is read, fetched, or exposed by this plan — it contains zero JSX/logic. |
| T-01-02 | Tampering | N/A | accept | No new write paths or forms introduced; this plan touches only static CSS/HTML assets served as-is. |
</threat_model>

<verification>
1. `cd frontend && npm run build` succeeds with no errors.
2. `grep -c "box-shadow" frontend/src/styles.css` returns `0` (no glow effects anywhere, per UI-SPEC "no box-shadow glows").
3. Both breakpoints present: `grep -c "@media (min-width: 900px)" frontend/src/styles.css` == 1 and `grep -c "@media (max-width: 600px)" frontend/src/styles.css` == 1.
4. Visually confirm (via `npm run dev`) that the page renders with dark background even though PlayerCard/MovieCard JSX has not changed yet — existing classes like `.player-card`, `.movie-grid` may look unstyled/broken until Plans 01-02/03/04 land; this is expected and not a regression since this plan does not touch JSX.
</verification>

<success_criteria>
- `frontend/index.html` loads Playfair Display, JetBrains Mono, and Inter (no Bebas Neue).
- `frontend/src/styles.css` defines every token and class listed in `must_haves.artifacts` and is free of the superseded rules listed in UI-SPEC's "Superseded Tokens" section.
- Locked line-heights (Label 1.4, Heading 1.2, Display 1.15) are tokenized and applied to their roles.
- `npm run build` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-redesign/01-01-SUMMARY.md`
</output>
